using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TesteScaler : MonoBehaviour
{
    /* private Vector3 cameraRightMiddle;

     private void Start()
     {
         cameraRightMiddle = Camera.main.ViewportToWorldPoint(new Vector2(1f, 0.5f));
         cameraRightMiddle.z = transform.position.z;
         transform.position = cameraRightMiddle;
     }*/

    /*float posZ;
    Camera cam;

    private void Start()
    {
        posZ = transform.position.z;
        cam = Camera.main;

        float height = (Mathf.Tan(cam.fov * Mathf.Deg2Rad * 0.5f) * posZ * 2f) / 10f;
        float width = height * cam.aspect;

    }*/
}
